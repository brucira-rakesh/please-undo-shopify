{% doc %}
  Single slide for PU product slider.

  @param {string} category - Category label
  @param {string} title - Product title
  @param {string} bullets_html - Feature bullets HTML
  @param {object} [product_image] - Image object
  @param {object} [figure_image] - Side illustration
  @param {string} shop_url - Shop now link
  @param {number} slide_index - Zero-based slide index
  @param {string} [shopify_attributes] - Theme editor block attributes
{% enddoc %}

<div
  class="pu-product-slider__slide{% if slide_index == 0 %} is-active{% endif %}"
  data-pu-product-slide
  data-index="{{ slide_index }}"
  {{ shopify_attributes }}
>
  <div class="pu-product-slider__content">
    <div class="pu-product-slider__info">
      <p class="pu-product-slider__category">{{ category }}</p>
      <h3 class="pu-product-slider__product-title">{{ title }}</h3>
      <div class="pu-product-slider__features">
        {% assign bullet_count = 0 %}
        {% assign bullets_stripped = bullets_html | replace: '<ol>', '' | replace: '</ol>', '' | replace: '<ul>', '' | replace: '</ul>', '' %}
        {% if bullets_stripped contains '<li>' %}
          {% assign bullet_parts = bullets_stripped | split: '<li>' %}
          {% for part in bullet_parts %}
            {% assign item_html = part | split: '</li>' | first | strip %}
            {% assign item_text = item_html | strip_html | strip %}
            {% if item_text != blank %}
              {% assign bullet_count = bullet_count | plus: 1 %}
              <div class="pu-product-slider__feature-row">
                <span class="pu-product-slider__feature-num">{{ bullet_count }}</span>
                <span class="pu-product-slider__feature-label">{{ item_html }}</span>
              </div>
            {% endif %}
          {% endfor %}
        {% else %}
          {% assign bullet_parts = bullets_html | split: '<p>' %}
          {% for part in bullet_parts %}
            {% assign item_html = part | split: '</p>' | first | strip %}
            {% assign item_text = item_html | strip_html | strip %}
            {% if item_text != blank %}
              {% assign bullet_count = bullet_count | plus: 1 %}
              <div class="pu-product-slider__feature-row">
                <span class="pu-product-slider__feature-num">{{ bullet_count }}</span>
                <span class="pu-product-slider__feature-label">{{ item_html }}</span>
              </div>
            {% endif %}
          {% endfor %}
        {% endif %}
        {% if bullet_count == 0 %}
          <div class="pu-product-slider__feature-row">
            <span class="pu-product-slider__feature-num">1</span>
            <span class="pu-product-slider__feature-label">Low smell</span>
          </div>
          <div class="pu-product-slider__feature-row">
            <span class="pu-product-slider__feature-num">2</span>
            <span class="pu-product-slider__feature-label">0 Stain</span>
          </div>
          <div class="pu-product-slider__feature-row">
            <span class="pu-product-slider__feature-num">3</span>
            <span class="pu-product-slider__feature-label">Daily use</span>
          </div>
        {% endif %}
      </div>
      <a href="{{ shop_url }}" class="pu-btn pu-btn--yellow">SHOP NOW</a>
    </div>

    <div class="pu-product-slider__stage">
      <div class="pu-product-slider__image-wrap">
        {% if product_image != blank %}
          {{
            product_image
            | image_url: width: 600
            | image_tag: class: 'pu-product-slider__product-img', loading: 'lazy', alt: title
          }}
        {% else %}
          <img
            src="{{ 'pu-product-mockup.png' | asset_url }}"
            alt="{{ title | escape }}"
            class="pu-product-slider__product-img"
            width="291"
            height="744"
            loading="lazy"
          >
        {% endif %}
      </div>
      {% if figure_image != blank %}
        {{
          figure_image
          | image_url: width: 400
          | image_tag: class: 'pu-product-slider__figure', loading: 'lazy', alt: ''
        }}
      {% else %}
        <img
          class="pu-product-slider__figure"
          src="{{ 'pu-stick-figure-2.png' | asset_url }}"
          alt=""
          width="274"
          height="332"
          loading="lazy"
        >
      {% endif %}
    </div>
  </div>
</div>
